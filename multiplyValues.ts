const a: number = 12;
const b: number = 3;

function multiplyValues(firstValue: number, secondValue: number): void {
  const product = firstValue * secondValue;
  console.log('product is ', product);
}

multiplyValues(a,b);